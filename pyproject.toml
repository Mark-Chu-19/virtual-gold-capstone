[project]
name = "virtual-gold-capstone"
version = "0.1.0"
description = "Confidence-scoring evaluation harness for the Virtual Gold hybrid AI assistant capstone"
requires-python = ">=3.10"

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

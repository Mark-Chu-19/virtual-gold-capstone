# Team To-Do

Single place for open decisions and pending actions. Tick items here; when a decision changes the architecture, update `docs/` in the same PR.
Fill in **Owner** and **Due** at the team meeting. Chinese version: [`TODO-zh.md`](TODO-zh.md).

Meeting cadence (US Eastern, all weekly): client Monday 18:00–19:00 · professor Tuesday 17:00–18:20 · team Friday 15:30–16:30.

## A. Decisions (A1–A4 decided 2026-09-15; A8 pending the client; A9 owners set 2026-09-18; A10 open points for 2026-09-21)

| # | Item | Notes | Owner | Due | Done |
|---|---|---|---|---|---|
| 1 | **Tool selection** (decided except the cloud SDK) | The client asked for a routing mechanism and confidence scoring, not for any particular tool (Sep 3 notes; SOW §2). Components and choices, decided 2026-09-15: **Local inference:** HF transformers, 8B at 4-bit via bitsandbytes on the T4; the harness needs per-token logprobs and hidden states (Semantic Entropy Probe, PR #2), which Ollama, llama.cpp server and vLLM do not expose. **Router:** our own Python module (confidence score plus sensitivity rule), calling transformers locally. **PII gate:** Presidio. **Cloud call:** the official SDK of the provider chosen in A6. Not used: Ollama (no hidden states); LiteLLM (an HTTP adapter over OpenAI-style APIs, nothing to adapt with transformers in-process; its hooks route pre-call, not on confidence). RouteLLM is a routing strategy (task-complexity classification), not a tool; it is covered by architecture §5 task heuristics and the §9 stretch goal. | Mark | W4 | [x] |
| 2 | **RAG knowledge layer** | **Decided 2026-09-15: not in the MVP; stretch goal.** The client has not asked for a knowledge base (Sep 3 notes, SOW); retrieval-grounded verification is one optional confidence strategy in the SOW, and PR #2 already scopes it out of the harness. No documents to index until the use case is set and the client provides data (A8). Revisit after the W7 midpoint if the client's data and time allow; F32 research (one-page summary) still goes ahead so the option stays open. | Mark | — | [x] |
| 3 | **Data sensitivity classification** | **Decided 2026-09-15: in the MVP.** The "confidential never leaves local" rule is the policy half of the SOW's "confidence-based or policy-based request routing" and directly implements the client's core requirement (protect proprietary data, Sep 3). One rule in our own router (A1): a request tagged confidential stays local regardless of confidence. Levels default to architecture §10 (public / internal / confidential) until the client answers B10. | Mark | — | [x] |
| 4 | **De-identification round trip** | **Decided 2026-09-15: both halves in the MVP, minimal form.** De-identification (Presidio redaction before any cloud call) is required by the client's privacy parameters. Re-identification is a per-request mapping table: record placeholder → original on the way out, string-replace on the way back; without it the cloud answer still contains placeholders and the CLI demo is unusable. Numeric data: placeholders in the MVP, format-preserving fake values stay a stretch goal (architecture §10 default). | Mark | — | [x] |
| 5 | **Local models and sandbox** | **Decided 2026-09-10: no LLMs on laptops.** Models run on a GPU sandbox that simulates on-premises; laptops are for development only. Models: Llama 3.1 8B primary, Qwen3 8B secondary. Serving: HF transformers with bitsandbytes 4-bit (A1); quantization is therefore settled, GGUF levels no longer apply. Still open: the sandbox itself and cloud credits, tracked in **B15** (do not duplicate here). When asking the client for the sandbox spec, note that a T4 (16 GB) runs the 8B model with hidden-state output but the k-sample ensemble (PR #2 Config C) is memory-tight; an A10G-class GPU (24 GB) makes that run comfortably (architecture §10). | Mark | W4 | [ ] |
| 6 | **Cloud provider: OpenAI or Anthropic** | **Rule set 2026-09-15: follows B15.** Use whichever provider the client supplies credits or an enterprise account for; if the client has no preference, default to Anthropic. The client has not named a provider (Sep 3 notes, SOW); both publish enterprise no-training commitments and are priced in the same band, and the harness only needs a `generate(query)` call on the cloud side (confidence is computed on the local model, so the cloud API's lack of logprobs does not matter; TruthfulQA scoring is MC1/MC2 only, PR #2). Decision closes when B15 is answered. | Mark | W4 | [ ] |
| 7 | **What to show at the W7 midpoint** | **Set 2026-09-15.** (1) Three configurations, local-only / hybrid / cloud-only, on the MMLU subset and GSM8K (finance set: FinQA sample, decided 2026-09-21): accuracy, escalation rate, cost per query, latency (PII-leak rate and prompt-injection resistance come in W9–W11). Cloud-only falls out of the same run because every query is sent to the cloud once and cached (PR #2). (2) Confidence signal: token logprob only (PR #2 variant A); SEP and the sampling ensemble in W9–W11. (3) One chart: escalation rate vs accuracy, the direct answer to "how much escalation is enough". (4) A CLI demo of one request containing PII going through redaction, escalation and re-identification (A3, A4). Deck ready in W6. | Mark | W6 | [ ] |
| 8 | **Use case / sector** | **Team proposal, 2026-09-18 (not yet a decision):** answer the client's "vertical or horizontal" with both, and take Inderpal's financial trading assistant as the vertical (public filings and market data may go to the cloud; positions and strategy stay local); the sanitization gateway is the horizontal capability. Worked examples in `meetings/team/2026-09-18/`. **To confirm with the client at the 2026-09-21 meeting**; the client may choose vertical, horizontal, or both. Karina's synthetic data (PRs #3–#5) is built on this proposal and adapts if the client redirects. Goes into the decision log only after the client confirms. | Mark | 2026-09-21 | [ ] |
| 9 | **Work split and research** | **Owners set at the 2026-09-18 team meeting:** (1) Router and integration: Mark · (2) Models and inference: Anmol · (3) Confidence scoring and harness: Zhexuan · (4) Security and de-identification: Yudi · (5) Synthetic data: Karina. Scope per workstream: (1) **Router and integration**: our own Python router (confidence threshold plus the confidential-stays-local rule, A3), cloud SDK call (A6), CLI entry point. (2) **Models and inference**: HF transformers + bitsandbytes 4-bit for Llama 3.1 8B and Qwen3 8B, sandbox setup (B12), throughput and memory on the sandbox GPU. (3) **Confidence scoring and evaluation harness**: owner Zhexuan Ye (research report and design doc delivered, PR #2); sampling core, benchmark loaders, metrics, midpoint pilot. (4) **Security and de-identification**: Presidio gate, re-identification mapping table (A4), PII-leak and prompt-injection evaluation in W9–W11. (5) **Synthetic data (RAG deferred to stretch, A2)**: synthetic enterprise queries and documents for the demo and B14, F32 one-pager; lightest load before W7, so this owner also supports (3) or the midpoint deck. Each workstream starts with the F-section research one-pager. Docs and slides: each owner writes their part, Mark consolidates. | Mark | — | [x] |
| 10 | **Sensitivity gate and evaluation: six open points, to settle at the 2026-09-21 meetings** (raised by Karina's PRs #3–#5 and Yudi's F31 report). (a) Query split: stretch (W9+) or midpoint? Karina's demo data assumes it; our answer to Alex said stretch. (b) Demo shape: PII inline in the question (PR #4 today) or a natural question plus a separate strategy-note document (`document_text`)? (c) Public data rule: rename `data/real/` to `data/public/`, fetch at runtime, keep it out of git; README wording "no client or personal data". (d) Entities in public-source documents (executive names in filings) are not PII and are not redacted; only internal/confidential sources count. Affects the PII-leak metric. (e) Numeric handling: placeholders (architecture §10 default) or Yudi's session-secret scaling factor? (f) PII-leak metric: detector F1 vs per-entity-type recall plus multi-turn leakage, per Yudi's report. Also: rename the gateway from "PII detection" to "sensitive-information detection" in v0.3.1. | Mark | 2026-09-21 | [ ] |

## B. Pending actions

| # | Action | Owner | Due | Done |
|---|---|---|---|---|
| 10 | **Walk the client through the architecture §10 defaults at the 2026-09-15 meeting** instead of sending them. Ask on three: cloud provider (A6: whichever they supply credits for, else Anthropic), who pays (B15), use case / sector (A8). Inform on the other seven: three sensitivity levels, automatic escalation with logging, Qwen3 as secondary with supply-chain checks, T4-class sandbox with 8B at 4-bit (now on HF transformers), placeholders for numeric data, NIST AI RMF, public datasets plus synthetic data. Proceed on defaults unless the client objects; record answers in D21–D23 after the meeting and fold them into v0.3 (B11). | Mark | 2026-09-15 | [ ] |
| 11 | **Architecture doc v0.3 published 2026-09-15** (files renamed to `architecture-v0.3-*`): §3/§4 RAG marked stretch, §7 midpoint target, §9 timeline and MVP list, §10 cloud-provider row, §11 and §12 rewritten, header and footer. **Remaining for v0.3.1, after PR #2 merges:** §5 add the verbalized-confidence exclusion and the signal priority (token logprob → SEP → sampling ensemble); §7 add the confidence-metrics sub-table. All four formats. | Mark | after PR #2 | [ ] |
| 12 | **Set up the sandbox** (blocked on B15). Install Python, PyTorch with CUDA, transformers, bitsandbytes, accelerate; download Llama 3.1 8B (accept Meta's licence on Hugging Face first) and Qwen3 8B. Acceptance: a smoke test that loads the 8B model at 4-bit and runs one `generate` returning text, per-token logprobs and hidden states, logging tokens/s and peak GPU memory; this test is the first step of the harness sampling core and is handed to workstream (3). Laptops get only the client code and a small dev model (Qwen3 0.6B or Llama 3.2 1B) so code runs end-to-end locally before it goes to the sandbox. | Anmol | W4 | [ ] |
| 13 | **Stand up the evaluation-harness skeleton** per PR #2. Runs architecture §7's three configurations (local-only / hybrid / cloud-only) and records its six system metrics (accuracy, latency, cost per query, escalation rate, PII-leak rate, prompt-injection resistance), plus the confidence sub-table (ECE, AUROC, AURC, escalation vs accuracy) for each signal variant. Every query goes to the cloud once and is cached, so cloud-only comes from the same run. W4 deliverables, three branches: `feat/harness-sampling-core` (shared k-sample loop on B12's smoke test), `eval/benchmark-loaders` (MMLU subset and GSM8K first, with B14), `eval/calibration-metrics` (unit-tested on synthetic data; no GPU needed, can start on laptops). Signal variant A and the midpoint pilot follow in W5–W6. | Zhexuan | W4 | [ ] |
| 14 | **Benchmark test sets** (same work as B13's loaders). MMLU: fixed number of questions per subject (e.g. 20 × 57 ≈ 1,140), fixed random seed, plus a separate dev split for threshold calibration (PR #2: thresholds are never tuned on the test set). GSM8K: official test split (1,319), dev cut from train. | Zhexuan | W4 | [ ] |
| 14a | **Synthetic enterprise data** (workstream 5). Two batches: (i) a dozen PII-bearing requests for the midpoint CLI demo (A7), due W5, drafted in PR #4 (open, shape under discussion); (ii) enterprise documents for the W9–W11 PII-leak and prompt-injection tests: fix the format and generation prompts in W5, generate in volume before W9. Sector background from A8. Anything the client shares (D22) is used only as a format template, never added to the dataset; no real data at any point (architecture §10). | Karina | W5 | [ ] |
| 15 | **Confirm compute and credits with the client at the 2026-09-15 meeting.** Ask: (a) the cloud sandbox offered on Sep 3: provider, GPU (T4 16 GB works; A10G 24 GB lets the k-sample confidence ensemble run comfortably, A5), direct access such as SSH (the model runs in-process with transformers, not behind an API), who pays; (b) cloud model API credits or keys and a budget cap, and whether they already have an OpenAI or Anthropic enterprise account (A6 follows that); (c) when the sandbox is available and whether it stays up through the end of the semester. **Internal fallback only, do not raise with the client** (SOW v2 and architecture §10 no longer mention it): request a GPU instance through CMU Public Cloud Services: consultation form with Randy as faculty contact, then email Randall Trzeciak with justification and cost estimate (T4-class VM, 100 GB, $100 cap), cc Randy. Needed by W4. | Mark | 2026-09-15 | [ ] |

## C. Corrections to the teammate Data & Architecture proposal

File: `reference/team/Virtual_Gold_Data_Architecture_Proposal_1.docx`

| # | Fix | Done |
|---|---|---|
| 16 | "Llama 3.3 8B" → Llama 3.1 8B (Llama 3.3 exists only at 70B) | [ ] |
| 17 | "Qwen3 7B" → Qwen3 8B (7B is Qwen2.5) | [ ] |
| 18 | Diagram: the "high confidence → return answer" arrow must not pass through the PII redaction gate | [ ] |
| 19 | Cite Meta and Qwen model cards for model figures and the NIST AI RMF text for the framework, instead of blog posts | [ ] |
| 20 | Align timeline with the course: W7 midpoint, W8 fall break, W14 Thanksgiving | [ ] |

## D. Waiting on the client

| # | Item | Asked on | Answer |
|---|---|---|---|
| 21 | Confirm or redirect the ten defaults in architecture §10 (walked through at the meeting, B10) | 2026-09-15 | |
| 22 | Example documents or formats for the "enterprise and small business data" in the brief, for the sector chosen in A8. Formats and samples only; no real data (B14a) | 2026-09-15 | |
| 23 | Confirm Qwen3 8B as the secondary model with supply-chain checks (architecture §10 item 4). On Sep 3 the client said international models face more security scrutiny and asked for modularity so models can be swapped; this is a confirmation, not a new question | 2026-09-15 | |

## E. Scope of Work before signing

`docs/sow/Scope of Work v2.docx` (changes in red) is done except the cost amount. Completed edits (sandbox assumption, sandbox dependency, Phase 1 prototyping sentence) are recorded in git history and no longer tracked here.

| # | Edit | Done |
|---|---|---|
| 26 | Section 6 cost ownership: the red paragraph states the Client covers costs with a $[X] placeholder. Ask the amount at the 2026-09-15 meeting (B15), fill it in, remove the "to be confirmed" note, then sign. The SOW does not mention the CMU fallback. | [ ] |

## F. Research list (by workstream)

Research deliverable for each workstream in W4: a one-page summary answering "can this tool or method be used in our architecture, how, and what are the limits", with source links. Read the official docs and paper abstracts first; no need to read every paper end to end.

| # | Workstream | Owner | What to research | Questions to answer |
|---|---|---|---|---|
| 28 | Router and integration | Mark | Threshold-based escalation and cascade rules: FrugalGPT (Chen et al. 2023, arXiv:2305.05176), Hybrid LLM quality-aware routing (Ding et al. 2024, arXiv:2404.14618), AutoMix self-verification routing (Madaan et al. 2023, arXiv:2310.12963). For the stretch comparison only: RouteLLM (Ong et al. 2024, arXiv:2406.18665), RouterBench (Hu et al. 2024, arXiv:2403.12031). LiteLLM dropped (A1). | How do the cascade papers set and tune the escalation rule, and what carries over to a single threshold on our confidence score? How does the confidential-stays-local rule (A3) stack with the confidence threshold in one router? What does a minimal Python router calling transformers locally and the cloud SDK look like? |
| 29 | Models and inference | Anmol | HF transformers + bitsandbytes on a T4: 4-bit vs 8-bit accuracy and speed for Llama 3.1 8B and Qwen3 8B; `generate` with `output_scores` and `output_hidden_states` (what the harness needs, PR #2); memory and throughput of multi-sample generation (`num_return_sequences=k`); accelerate device-map options. Official model cards and licence terms for both models. | Tokens/s and peak memory for one 8B model at 4-bit on a T4, single sample and k=5? Does 8-bit fit alongside hidden-state output? Which layer's hidden state does the SEP paper use? Does the Qwen3 licence restrict enterprise use? |
| 30 | Confidence scoring and evaluation harness | Zhexuan | **Delivered 2026-09-13/14:** literature review (`docs/research/confidence-scoring-harness-report-en.md`) and design doc (`docs/research/confidence-harness-design-en.md`, merged 2026-09-21). Covered self-consistency, semantic entropy and SEPs, Kadavath et al., SelfCheckGPT, Xiong et al., calibration metrics, and dataset scoring for MMLU, GSM8K, TruthfulQA, HaluEval. | Answered: which signals work at 8B (token logprob and SEP; verbalized confidence excluded), what each costs, and how the harness produces architecture §7's three configurations and six system metrics plus the confidence sub-table in one pass. |
| 31 | Security and de-identification | Yudi | **Delivered 2026-09-19:** `docs/research/security-deidentification-report-en.docx` (17-paper review: sensitivity ≠ PII, two-score policy instead of a binary gate, three sanitization strategies, four-layer classification, multi-turn leakage testing); client briefing in `meetings/client/2026-09-21/`. Original list: Presidio recognizers, custom entities, evaluation module. Privacy-conscious delegation, PAPILLON (Siyan et al. 2024, arXiv:2410.17127). OWASP Top 10 for LLM Applications. Indirect prompt injection (Greshake et al. 2023, arXiv:2302.12173). NIST AI RMF 1.0 and the Generative AI Profile (NIST AI 600-1). Re-identification: per-request placeholder mapping (A4), how existing tools (e.g. Presidio anonymizer operators) handle the reverse step. | Roughly what miss rate does Presidio have on synthetic enterprise documents? What is the simplest reliable way to map placeholders back after the cloud reply, and where does it break (reformatted or paraphrased placeholders)? Which NIST items does our risk assessment map to? |
| 32 | Synthetic data (RAG as stretch) | Karina | **Delivered 2026-09-20:** `docs/research/financial-data-sources-en.md` (FinQA adopted, decision log 2026-09-21). Remaining: methods for generating synthetic enterprise documents and Q&A with an LLM, seeded from the support-ticket and B2B datasets (architecture §7); how to plant PII and sensitivity labels deliberately so the security tests have ground truth (B14a). Stretch, one page only: Chroma vs pgvector, chunking, a locally runnable embedding model (bge, nomic-embed), RAGAS faithfulness as a grounding signal (A2). | How much synthetic data do the demo and the W9–W11 security tests need, and how are sensitivity levels labelled? What prompt and checks keep generated PII realistic but fake? For stretch: what is the scope of a minimal RAG buildable in one day? |

## Decision log

Record decisions here so nobody reopens them.

| Date | Decision | Rationale |
|---|---|---|
| 2026-09-03 | Client kickoff decisions: hybrid architecture with a mandatory local model; evaluate with general-purpose benchmarks and show performance comparable to cloud; local model enhancement in scope; no GUI; client provides a cloud sandbox; weekly meetings during discovery | Sep 3 client meeting notes (`meetings/client/2026-09-03/`) |
| 2026-09-07 | Local-first hybrid architecture with confidence-based escalation and sensitivity gating (architecture v0.1) | Matches the client brief |
| 2026-09-08 | Adopt teammate proposal's three-configuration evaluation, named datasets, defaults-instead-of-questions style, MVP vs stretch split (architecture v0.2) | More concrete and matches client's preference for defaults |
| 2026-09-08 | Timeline follows the course's 15-week structure without change | Midpoint W7 and breaks are fixed by the course |
| 2026-09-10 | No local LLM on laptops. Models run on a cloud GPU sandbox that simulates an on-premises environment; laptops are for development only | Laptops cannot run 8B models at benchmark speed; the client offered a sandbox on Sep 3; a VM we control still satisfies the "local" requirement |
| 2026-09-14 | Confidence-scoring MVP excludes verbalized confidence; prioritizes token-probability confidence and Semantic Entropy Probes over self-consistency/full semantic entropy | 2026 screen of seven 3B–9B open-weight models found verbalized confidence saturates (91.7% of responses self-report ≥95% confidence regardless of correctness); see `docs/research/confidence-harness-design-en.md` and `docs/research/confidence-scoring-harness-report-en.md` |
| 2026-09-15 | Local inference and the evaluation harness run on HF transformers (bitsandbytes 4-bit); Ollama is not used | The harness needs per-token logprobs and hidden states for the Semantic Entropy Probe (PR #2); one stack for harness and router avoids threshold drift between quantization formats |
| 2026-09-15 | RAG knowledge layer is a stretch goal, not in the MVP | Not in the client's requirements or evaluation criteria; no client data yet (A8); retrieval grounding already out of the confidence harness (PR #2). Reconsider after the W7 midpoint |
| 2026-09-15 | Data sensitivity classification is in the MVP: confidential requests never leave local, regardless of confidence | Policy half of the SOW's routing requirement; the client's core motivation is protecting proprietary data; one rule in the router |
| 2026-09-15 | De-identification and re-identification both in the MVP; re-identification is a per-request placeholder mapping table; numeric data uses placeholders, fake values are stretch | Redaction is required by the client's privacy parameters; without re-identification the cloud answer is unusable; the mapping table is a one-day task |
| 2026-09-21 | FinQA (MIT) is the finance QA benchmark for the accuracy_benchmark batch; FinanceBench (CC BY-NC) stays in reserve for coursework only | Karina's research (`docs/research/financial-data-sources-en.md`): zero licensing risk for the client, and FinQA's ground truth includes the reasoning program, so both the final number and the reasoning can be scored |
